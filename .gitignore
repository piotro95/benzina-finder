# Cache dati MIMIT (si riscaricano da soli)
data_cache/
# Output report
output/
# Python
__pycache__/
*.pyc
.venv/
venv/
.env
# IDE / OS
.vscode/
.idea/
.DS_Store

# Config locale con posizione reale (uso da PC, NON versionare)
config.local.yaml

# Automazione locale (Task Scheduler) — credenziali e stato, NON versionare
secrets.local.ps1
.last_run
run_daily.log
